<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <link rel="stylesheet" href="/svgs.css">
    <link rel="stylesheet" href="/membershipStyle.css">
    <title><%= title %></title>
    <link rel="icon" href="/favicon.ico" type="image/x-icon">
</head>
<body>
    <div class="global-wrapper">
        <div class="content">
            <h1>Membership</h1>
            <form action="/user/<%= userId %>/membership" method="POST">
                <div>
                    <label for="secret">Enter the <i>secret</i> code</label>
                    <input id="secret" name="secret" type="text" />
                    <% if (warning) { %>
                        <p><%= warning %></p>
                    <% } %>
                </div>
                <button type="submit" class="btn form-btn">Confirm</button>
            </form>
            <div class="link-options">
                <div>
                    <a href="/user/<%= userId %>/all-posts">Dashboard</a>
                </div>
            </div>
        </div>
    </div>
</body>
</html>